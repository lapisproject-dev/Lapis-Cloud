// Generated by kuml-codegen-m2m-exposed (erm-to-exposed) — do not edit manually.

package network.lapis.cloud.server.db.generated

import kotlin.uuid.Uuid
import kotlinx.datetime.LocalDate
import kotlinx.datetime.LocalDateTime
import network.lapis.cloud.shared.domain.VolunteerAllowanceCategory
import network.lapis.cloud.shared.domain.VolunteerAllowanceDeclarationSource
import org.jetbrains.exposed.v1.core.Column
import org.jetbrains.exposed.v1.core.Table
import org.jetbrains.exposed.v1.datetime.date
import org.jetbrains.exposed.v1.datetime.datetime

public object VolunteerAllowanceSelfDeclarationTable : Table("volunteer_allowance_self_declaration") {
    public val id: Column<Uuid> = uuid("id")
    public val memberId: Column<Uuid> = reference("member_id", MemberTable.id)
    public val category: Column<VolunteerAllowanceCategory> = enumerationByName<VolunteerAllowanceCategory>("category", 10)
    public val calendarYear: Column<Int> = integer("calendar_year")
    // Named `declarationSource`, not `source` -- Exposed's `Table`/`ColumnSet` already declares a
    // `source` member (the underlying SQL source for joins), so a same-named `val` here would hide
    // it without `override` (VIRTUAL_MEMBER_HIDDEN). The DB column itself is still literally
    // `source` (see V30__volunteer_allowance.sql) -- only the Kotlin property is renamed.
    public val declarationSource: Column<VolunteerAllowanceDeclarationSource> =
        enumerationByName<VolunteerAllowanceDeclarationSource>("source", 8)
    public val declaredAt: Column<LocalDateTime> = datetime("declared_at")
    public val signedOn: Column<LocalDate?> = date("signed_on").nullable()
    public val recordedBy: Column<Uuid> = reference("recorded_by", MemberTable.id)

    override val primaryKey: PrimaryKey = PrimaryKey(id)

    // Note: 2 index(es) declared on this entity are not emitted —
    // Exposed's index {} DSL needs typed column references, not wired up in this wave.

    // Note: 3 check constraint(s) declared on this entity are not
    // emitted — Exposed's check {} DSL needs a typed Op<Boolean>, not a raw SQL string.
}
